# .env.example - Variables d'environnement pour Hippique-Analyse-0925
# 
# INSTRUCTIONS :
# 1. Copier ce fichier : cp .env.example .env
# 2. Remplir les valeurs ci-dessous
# 3. NE JAMAIS commiter le fichier .env (il est dans .gitignore)
#
# ============================================

# ============================================
# 🔴 OBLIGATOIRES - Cloud Run & Scheduling
# ============================================

# Identifiant du projet Google Cloud
PROJECT_ID=

# Région Cloud Run / Cloud Tasks (ex: europe-west1)
REGION=europe-west1

# Nom du service Cloud Run
SERVICE_NAME=hippique-analyse

# Nom de la file Cloud Tasks
QUEUE_ID=hippique-run-queue

# URL publique du service Cloud Run (https://...a.run.app)
SERVICE_URL=

# Audience OIDC (laisser vide pour utiliser SERVICE_URL)
SERVICE_AUDIENCE=

# Compte de service utilisé par Cloud Tasks pour appeler le service
TASKS_SERVICE_ACCOUNT_EMAIL=

# Compte de service utilisé par Cloud Scheduler
SCHEDULER_SERVICE_ACCOUNT_EMAIL=

# Forcer l'authentification OIDC (true/false)
REQUIRE_AUTH=false

# Répertoire local de travail pour les artefacts
DATA_DIR=data/runtime

# User-Agent personnalisé pour les requêtes externes
HTTP_USER_AGENT=Hippique-Analyse/1.0 (+https://cloud.run/hippique)

# ============================================
# 🔴 OBLIGATOIRES - Google Cloud Storage
# ============================================

# Nom du bucket GCS pour stocker les données
# Exemple: hippique-analyse-prod
GCS_BUCKET=

# Clé de service Google Cloud encodée en base64
# Comment obtenir :
# 1. Télécharger credentials.json depuis GCP Console
# 2. Encoder : base64 -w 0 credentials.json
# 3. Coller le résultat ici
GCS_SERVICE_KEY_B64=

# Préfixe/dossier dans le bucket (optionnel)
# Exemple: hippiques/prod ou data/2025
GCS_PREFIX=

# ID du projet Google Cloud (optionnel)
# Exemple: hippique-analyse-123456
GOOGLE_CLOUD_PROJECT=

# ============================================
# 🔴 OBLIGATOIRES - Authentification ZEturf
# ============================================

# Identifiant ZEturf pour la connexion
ZETURF_LOGIN=

# Mot de passe ZEturf
ZETURF_PASSWORD=

# ============================================
# 🟡 OPTIONNELLES - Sources de données
# ============================================

# Cookie de session Geny.com (optionnel)
# Comment obtenir :
# 1. Se connecter sur geny.com
# 2. F12 > Application > Cookies
# 3. Copier la valeur du cookie de session
GENY_COOKIE=

# ============================================
# 🟡 OPTIONNELLES - Configuration pipeline
# ============================================

# Autoriser les heuristiques de backup (0=non, 1=oui)
# Par défaut: 0 (désactivé)
ALLOW_HEURISTIC=0

# Budget total par course (en euros)
# Par défaut: 5.0
BUDGET_TOTAL=5.0

# Ratio pour les paris simples (0.0 à 1.0)
# Par défaut: 0.6 (60%)
SP_RATIO=0.6

# Ratio pour les combinés (0.0 à 1.0)
# Par défaut: 0.4 (40%)
COMBO_RATIO=0.4

# Budget nominal pour la sélection H-5 (5€ par défaut)
GPI_BUDGET=5

# EV minimale pour les paris simples
# Par défaut: 0.35 (35%)
EV_MIN_SP=0.35
# Seuil global EV pour la sélection automatique (40% recommandé)
EV_MIN=0.40

# EV minimale globale pour les combinés
# Par défaut: 0.35 (35%)
EV_MIN_GLOBAL=0.35

# ROI minimal pour les paris simples
# Par défaut: 0.10 (10%)
ROI_MIN_SP=0.10
# Seuil ROI SP pour les rapports H-5 (20% recommandé)
ROI_SP_MIN=0.20

# ROI minimal global
# Par défaut: 0.25 (25%)
ROI_MIN_GLOBAL=0.25

# Payout minimal pour les combinés (en euros)
# Par défaut: 12.0
MIN_PAYOUT_COMBOS=12.0
# Payout minimal attendu pour H-5 (10€ recommandé)
PAYOUT_MIN=10

# Fraction de Kelly à utiliser
# Par défaut: 0.5 (50% de Kelly pour réduire variance)
KELLY_FRACTION=0.5

# Cap Kelly par cheval (% du budget)
# Par défaut: 0.60 (60%)
KELLY_CAP=0.60

# Mise minimale pour paris simples (en euros)
# Par défaut: 0.10
MIN_STAKE_SP=0.10

# Arrondi des mises SP (en euros, 0 pour désactiver)
# Par défaut: 0.10
ROUND_TO_SP=0.10

# Ratio de Sharpe minimal
# Par défaut: 0.5
SHARPE_MIN=0.5

# Coefficient de drift des cotes
# Par défaut: 0.05
DRIFT_COEF=0.05

# Coefficient bonus Jockey/Entraîneur
# Par défaut: 0.001
JE_BONUS_COEF=0.001

# Nombre maximum de tickets simples
# Par défaut: 2
MAX_TICKETS_SP=2

# Overround maximal (standard)
# Par défaut: 1.30
OVERROUND_MAX=1.30

# Overround maximal pour handicaps
# Par défaut: 1.25
OVERROUND_MAX_HANDICAP=1.25

# Autoriser l'absence de stats J/E (0=non, 1=oui)
# Par défaut: 0
ALLOW_JE_NA=0

# Échantillons minimaux pour p_true
# Par défaut: 100
P_TRUE_MIN_SAMPLES=100

# ============================================
# 🟡 OPTIONNELLES - Notifications
# ============================================

# Webhook Slack pour les notifications (optionnel)
SLACK_WEBHOOK=

# Email pour notifications (optionnel)
NOTIFICATION_EMAIL=

# ============================================
# 🟡 OPTIONNELLES - Dépôt PyPI privé
# ============================================

# URL d'un dépôt PyPI privé (optionnel)
# Exemple: https://pypi.company.com/simple
PYPI_EXTRA_INDEX=

# ============================================
# 🟡 OPTIONNELLES - Chemins personnalisés
# ============================================

# Chemin vers le fichier Excel de suivi
# Par défaut: excel/modele_suivi_courses_hippiques.xlsx
EXCEL_PATH=excel/modele_suivi_courses_hippiques.xlsx

# Chemin vers le dossier de données
# Par défaut: data
DATA_DIR=data

# Chemin vers le fichier de calibration
# Par défaut: calibration/payout_calibration.yaml
CALIBRATION_FILE=calibration/payout_calibration.yaml
# Surcharge éventuelle pour simulate_wrapper
CALIB_PATH=config/payout_calibration.yaml

# ============================================
# 🟡 OPTIONNELLES - Développement
# ============================================

# Mode debug (0=non, 1=oui)
DEBUG=0

# Niveau de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Timezone
TZ=Europe/Paris

# ============================================
# 🟢 POUR GITHUB ACTIONS UNIQUEMENT
# ============================================
# Ces variables sont définies dans GitHub Secrets/Variables
# Ne pas les définir ici localement

# MEETING_URLS=https://www.zeturf.fr/...
# COURSES_URLS=https://www.zeturf.fr/...
# GH_PAT=ghp_...

# ============================================
# EXEMPLES DE CONFIGURATION COMPLÈTE
# ============================================

# Configuration CONSERVATRICE (faible risque) :
# BUDGET_TOTAL=3.0
# SP_RATIO=0.7
# COMBO_RATIO=0.3
# KELLY_FRACTION=0.3
# MIN_PAYOUT_COMBOS=15.0
# SHARPE_MIN=0.7

# Configuration AGRESSIVE (risque élevé) :
# BUDGET_TOTAL=10.0
# SP_RATIO=0.5
# COMBO_RATIO=0.5
# KELLY_FRACTION=0.7
# MIN_PAYOUT_COMBOS=8.0
# SHARPE_MIN=0.3

# Configuration ÉQUILIBRÉE (recommandée) :
# BUDGET_TOTAL=5.0
# SP_RATIO=0.6
# COMBO_RATIO=0.4
# KELLY_FRACTION=0.5
# MIN_PAYOUT_COMBOS=12.0
# SHARPE_MIN=0.5

# ============================================
# FIN DU FICHIER
# ============================================
