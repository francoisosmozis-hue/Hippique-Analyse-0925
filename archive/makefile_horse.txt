# ============================================================================
# Makefile - Orchestrateur Hippique Cloud Run
# ============================================================================
# Usage: make [target]
# ============================================================================

.PHONY: help setup deploy scheduler test logs clean

# Charger variables depuis .env
ifneq (,$(wildcard .env))
    include .env
    export
endif

# Couleurs
RED := \033[0;31m
GREEN := \033[0;32m
YELLOW := \033[1;33m
BLUE := \033[0;34m
NC := \033[0m

# Variables par défaut
PROJECT_ID ?= $(shell gcloud config get-value project)
REGION ?= europe-west1
SERVICE_NAME ?= horse-racing-orchestrator

help: ## Afficher l'aide
	@echo "$(BLUE)🐴 Orchestrateur Hippique - Commandes disponibles$(NC)"
	@echo "=================================================="
	@grep -E '^[a-zA-Z_-]+:.*?## .*$$' $(MAKEFILE_LIST) | \
		awk 'BEGIN {FS = ":.*?## "}; {printf "  $(GREEN)%-20s$(NC) %s\n", $$1, $$2}'
	@echo ""

setup: ## Configuration initiale (APIs, SA, queue)
	@echo "$(BLUE)🔧 Configuration initiale...$(NC)"
	@echo "Project: $(PROJECT_ID)"
	@echo "Region: $(REGION)"
	@echo ""
	@echo "$(YELLOW)Activation des APIs...$(NC)"
	@gcloud services enable \
		run.googleapis.com \
		cloudtasks.googleapis.com \
		cloudscheduler.googleapis.com \
		cloudbuild.googleapis.com \
		storage.googleapis.com \
		--project=$(PROJECT_ID)
	@echo "$(GREEN)✅ APIs activées$(NC)"
	@echo ""
	@echo "$(YELLOW)Vérification Service Account...$(NC)"
	@if ! gcloud iam service-accounts describe $(SERVICE_NAME)@$(PROJECT_ID).iam.gserviceaccount.com --project=$(PROJECT_ID) 2>/dev/null; then \
		gcloud iam service-accounts create $(SERVICE_NAME) \
			--display-name="Horse Racing Orchestrator" \
			--project=$(PROJECT_ID); \
		echo "$(GREEN)✅ Service Account créé$(NC)"; \
	else \
		echo "$(GREEN)✅ Service Account existe$(NC)"; \
	fi
	@echo ""
	@echo "$(GREEN)✅ Setup terminé$(NC)"

build: ## Build de l'image Docker localement
	@echo "$(BLUE)🔨 Build local...$(NC)"
	docker build -t $(SERVICE_NAME):local .
	@echo "$(GREEN)✅ Image buildée: $(SERVICE_NAME):local$(NC)"

run-local: build ## Lancer le service localement
	@echo "$(BLUE)🚀 Démarrage local...$(NC)"
	docker run -p 8080:8080 \
		-e PROJECT_ID=$(PROJECT_ID) \
		-e REGION=$(REGION) \
		-e REQUIRE_AUTH=false \
		--env-file .env \
		$(SERVICE_NAME):local

deploy: ## Déployer sur Cloud Run
	@echo "$(BLUE)🚀 Déploiement Cloud Run...$(NC)"
	@chmod +x scripts/deploy_cloud_run.sh
	@./scripts/deploy_cloud_run.sh
	@echo "$(GREEN)✅ Déploiement terminé$(NC)"

deploy-fast: ## Déploiement sans rebuild (utilise dernière image)
	@echo "$(BLUE)⚡ Déploiement rapide (sans rebuild)...$(NC)"
	@chmod +x scripts/deploy_cloud_run.sh
	@./scripts/deploy_cloud_run.sh --no-build
	@echo "$(GREEN)✅ Déploiement terminé$(NC)"

scheduler: ## Créer le job Scheduler quotidien 09:00
	@echo "$(BLUE)⏰ Configuration Scheduler...$(NC)"
	@chmod +x scripts/create_scheduler_0900.sh
	@./scripts/create_scheduler_0900.sh
	@echo "$(GREEN)✅ Scheduler configuré$(NC)"

test-local: ## Tests locaux (sans authentification)
	@echo "$(BLUE)🧪 Tests locaux...$(NC)"
	@chmod +x scripts/test_local.sh
	@./scripts/test_local.sh --no-auth

test-prod: ## Tests en production (avec authentification)
	@echo "$(BLUE)🧪 Tests production...$(NC)"
	@if [ -z "$(SERVICE_URL)" ]; then \
		echo "$(RED)❌ SERVICE_URL non défini dans .env$(NC)"; \
		exit 1; \
	fi
	@chmod +x scripts/test_local.sh
	@./scripts/test_local.sh

logs: ## Afficher les logs Cloud Run (dernières 50 entrées)
	@echo "$(BLUE)📋 Logs Cloud Run...$(NC)"
	@gcloud logging read \
		"resource.type=cloud_run_revision AND resource.labels.service_name=$(SERVICE_NAME)" \
		--limit 50 \
		--format json \
		--project=$(PROJECT_ID)

logs-errors: ## Afficher uniquement les erreurs
	@echo "$(BLUE)🔍 Erreurs uniquement...$(NC)"
	@gcloud logging read \
		"resource.type=cloud_run_revision AND resource.labels.service_name=$(SERVICE_NAME) AND severity>=ERROR" \
		--limit 30 \
		--format json \
		--project=$(PROJECT_ID)

logs-scheduler: ## Logs du Scheduler
	@echo "$(BLUE)📅 Logs Scheduler...$(NC)"
	@gcloud logging read \
		"resource.type=cloud_scheduler_job" \
		--limit 20 \
		--project=$(PROJECT_ID)

tasks-list: ## Lister les tâches Cloud Tasks en attente
	@echo "$(BLUE)📋 Tâches en attente...$(NC)"
	@gcloud tasks list \
		--queue=horse-racing-queue \
		--location=$(REGION) \
		--project=$(PROJECT_ID)

scheduler-list: ## Lister les jobs Scheduler
	@echo "$(BLUE)📅 Jobs Scheduler...$(NC)"
	@gcloud scheduler jobs list \
		--location=$(REGION) \
		--project=$(PROJECT_ID)

trigger-schedule: ## Déclencher manuellement le planning du jour
	@echo "$(BLUE)🎯 Déclenchement manuel du planning...$(NC)"
	@if [ -z "$(SERVICE_URL)" ]; then \
		echo "$(RED)❌ SERVICE_URL non défini dans .env$(NC)"; \
		exit 1; \
	fi
	@TOKEN=$$(gcloud auth print-identity-token --audiences=$(SERVICE_URL)); \
	curl -X POST \
		-H "Authorization: Bearer $$TOKEN" \
		-H "Content-Type: application/json" \
		-d '{"date":"today","mode":"tasks"}' \
		$(SERVICE_URL)/schedule | jq .
	@echo "$(GREEN)✅ Planning déclenché$(NC)"

trigger-scheduler-job: ## Exécuter le job Scheduler manuellement
	@echo "$(BLUE)⏰ Exécution manuelle du job Scheduler...$(NC)"
	@gcloud scheduler jobs run daily-plan-0900 \
		--location=$(REGION) \
		--project=$(PROJECT_ID)
	@echo "$(GREEN)✅ Job exécuté$(NC)"

describe: ## Afficher les infos du service Cloud Run
	@echo "$(BLUE)ℹ️  Informations du service...$(NC)"
	@gcloud run services describe $(SERVICE_NAME) \
		--region=$(REGION) \
		--project=$(PROJECT_ID)

url: ## Afficher l'URL du service
	@gcloud run services describe $(SERVICE_NAME) \
		--region=$(REGION) \
		--project=$(PROJECT_ID) \
		--format='value(status.url)'

status: ## Statut complet (service + tasks + scheduler)
	@echo "$(BLUE)📊 Statut complet$(NC)"
	@echo "=================================================="
	@echo ""
	@echo "$(YELLOW)Service Cloud Run:$(NC)"
	@gcloud run services describe $(SERVICE_NAME) \
		--region=$(REGION) \
		--project=$(PROJECT_ID) \
		--format='table(status.url,status.conditions[0].status,metadata.creationTimestamp)' 2>/dev/null || \
		echo "  $(RED)❌ Service non déployé$(NC)"
	@echo ""
	@echo "$(YELLOW)Queue Cloud Tasks:$(NC)"
	@gcloud tasks queues describe horse-racing-queue \
		--location=$(REGION) \
		--project=$(PROJECT_ID) \
		--format='table(name,state)' 2>/dev/null || \
		echo "  $(RED)❌ Queue non créée$(NC)"
	@echo ""
	@echo "$(YELLOW)Tâches en attente:$(NC)"
	@gcloud tasks list --queue=horse-racing-queue --location=$(REGION) --project=$(PROJECT_ID) \
		--format='table(name,scheduleTime)' 2>/dev/null || \
		echo "  Aucune tâche"
	@echo ""
	@echo "$(YELLOW)Jobs Scheduler:$(NC)"
	@gcloud scheduler jobs list --location=$(REGION) --project=$(PROJECT_ID) \
		--format='table(name,schedule,state)' 2>/dev/null || \
		echo "  $(RED)❌ Aucun job$(NC)"

clean-tasks: ## Supprimer toutes les tâches en attente
	@echo "$(YELLOW)⚠️  Suppression de toutes les tâches...$(NC)"
	@read -p "Êtes-vous sûr? [y/N] " -n 1 -r; \
	echo; \
	if [[ $$REPLY =~ ^[Yy]$$ ]]; then \
		gcloud tasks queues purge horse-racing-queue \
			--location=$(REGION) \
			--project=$(PROJECT_ID); \
		echo "$(GREEN)✅ Tâches supprimées$(NC)"; \
	else \
		echo "$(YELLOW)⏭️  Annulé$(NC)"; \
	fi

clean-scheduler: ## Supprimer le job Scheduler
	@echo "$(YELLOW)⚠️  Suppression du job Scheduler...$(NC)"
	@read -p "Êtes-vous sûr? [y/N] " -n 1 -r; \
	echo; \
	if [[ $$REPLY =~ ^[Yy]$$ ]]; then \
		gcloud scheduler jobs delete daily-plan-0900 \
			--location=$(REGION) \
			--project=$(PROJECT_ID) \
			--quiet; \
		echo "$(GREEN)✅ Job supprimé$(NC)"; \
	else \
		echo "$(YELLOW)⏭️  Annulé$(NC)"; \
	fi

clean: clean-tasks ## Nettoyer (tâches seulement)
	@echo "$(GREEN)✅ Nettoyage terminé$(NC)"

destroy: ## Supprimer TOUT (service + queue + scheduler)
	@echo "$(RED)⚠️  ATTENTION: Suppression complète !$(NC)"
	@read -p "Taper 'DELETE' pour confirmer: " confirm; \
	if [ "$$confirm" = "DELETE" ]; then \
		echo "$(YELLOW)Suppression du service...$(NC)"; \
		gcloud run services delete $(SERVICE_NAME) --region=$(REGION) --project=$(PROJECT_ID) --quiet || true; \
		echo "$(YELLOW)Suppression du scheduler...$(NC)"; \
		gcloud scheduler jobs delete daily-plan-0900 --location=$(REGION) --project=$(PROJECT_ID) --quiet || true; \
		echo "$(YELLOW)Suppression de la queue...$(NC)"; \
		gcloud tasks queues delete horse-racing-queue --location=$(REGION) --project=$(PROJECT_ID) --quiet || true; \
		echo "$(RED)✅ Tout supprimé$(NC)"; \
	else \
		echo "$(YELLOW)⏭️  Annulé$(NC)"; \
	fi

.DEFAULT_GOAL := help
