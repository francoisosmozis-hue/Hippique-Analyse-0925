[pytest]
# Où chercher les tests
testpaths =
    tests
    scripts/tests

# Ne pas descendre dans ces dossiers
norecursedirs =
    .git
    .venv
    data
    out
    Hippique-Analyse-0925

# Ajouter le projet (racine) au PYTHONPATH
pythonpath = .

# Options par défaut (mode silencieux ici)
addopts = -q
